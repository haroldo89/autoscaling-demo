apiVersion: v1
kind: Service
metadata:
  name: service-deploy-nginx
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
  - name: http-web-svc
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30808